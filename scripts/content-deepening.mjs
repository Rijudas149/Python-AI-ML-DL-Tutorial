/**
 * Optional topic deepening — disabled to avoid generic appended sections.
 * Source curriculum sections carry the teaching content.
 */

export function deepenTopicContent(topic) {
  return topic;
}
